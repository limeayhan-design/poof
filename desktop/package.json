{
  "name": "poof-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Poof native desktop shell (Tauri). Wraps the PWA and adds LAN mDNS discovery.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev --no-watch",
    "build": "tauri build",
    "icon": "tauri icon"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1",
    "@tauri-apps/plugin-clipboard-manager": "^2.0.1",
    "@tauri-apps/plugin-dialog": "^2.0.1",
    "@tauri-apps/plugin-fs": "^2.0.1",
    "@tauri-apps/plugin-global-shortcut": "^2.0.1",
    "@tauri-apps/plugin-opener": "^2.0.0"
  }
}
